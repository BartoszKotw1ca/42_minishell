2. Learn how branches works.
3. Read about functions:
readline, rl_clear_history, rl_on_new_line,
rl_replace_line, rl_redisplay, add_history,
printf, malloc, free, write, access, open, read,
close, fork, wait, waitpid, wait3, wait4, signal,
sigaction, sigemptyset, sigaddset, kill, exit,
getcwd, chdir, stat, lstat, fstat, unlink, execve,
dup, dup2, pipe, opendir, readdir, closedir,
strerror, perror, isatty, ttyname, ttyslot, ioctl,
getenv, tcsetattr, tcgetattr, tgetent, tgetflag,
tgetnum, tgetstr, tgoto, tputs
4. Import pipex. 
5. Make Makefile(pipex, libft); 
6. 

Kiedy cos takiego wpiszemy w program:
< janek.txt cat | grep kot | awk '{print}' | cmd4 > end.txt asdf
output jest taki: (niepoprawna komenda oraz outfile)
data.infile = janek.txt
data.outfile = (null)
com: cat  
com:  grep kot  
com:  awk '{print}'  
com:  cmd4 > end.txt asdf
